export interface DiscoverySetContent {
  heading: string;
  subtitle: string; // e.g. "TRIPLE TESTER SET"
  priceLabel: string; // e.g. "Rs. 1,000"
  ctaLabel: string;
  ctaHref: string;
  backgroundImage: string;
}